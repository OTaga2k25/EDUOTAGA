# Base URL of the deployed EDUOTAGA website (Next.js API routes).
# During local development, point this at your machine's LAN IP so a
# physical device / simulator can reach it — "localhost" only works
# inside the same machine's browser.
EXPO_PUBLIC_API_URL=http://localhost:3000
